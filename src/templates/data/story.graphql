query {
  categories: getBlog {
    categories {
      title
    }
  }
  featuredCategories: getCategoryList {
    items {
      title
      storySet(size: 6) {
        items {
          _contentTypeName
          _enabledAt
          title
          slug
          social {
            title
          }
          category {
            title
          }
          location {
            title
          }
          tout {
            image {
              path
            }
            dek
          }
        }
      }
    }
  }
  featuredCategoryCandidates: getCategoryList {
    items {
      title
      storySet(size: 12, sort: [{field: "_enabledAt", order: "desc"}]) {
        items {
          _contentTypeName
          _enabledAt
          title
          slug
          category { title }
          tout {
            image { path }
            dek
          }
        }
      }
    }
  }
  recentStories: getStoryList(size: 8, sort: [{field: "_enabledAt", order: "desc"}]) {
    items {
      _contentTypeName
      title
      slug
      social { title }
      tout { image { path } }
    }
  }
  page: getBlog {
    _contentTypeName
    title
    magazineKicker
    magazineHeadline
    magazineIntroduction
    featuredStory {
      _contentTypeName
      _enabledAt
      title
      slug
      category {
        title
      }
      tout {
        image {
          path
        }
        dek
      }
      shopEditTitle
      shopEditEnabled
      shopEditItems {
        image { path }
        title
        productUrl
      }
    }
    supportingStories {
      _contentTypeName
      _enabledAt
      title
      slug
      category { title }
      tout { image { path } dek }
    }
    fullBleedStory {
      _contentTypeName
      title
      slug
      category { title }
      tout { image { path } dek }
    }
    shoppingStory {
      _contentTypeName
      title
      slug
      shopEditTitle
      shopEditEnabled
      shopEditItems {
        image { path }
        title
        productUrl
      }
    }
    categories {
      title
      storySet(size: 10, sort: [{field: "_enabledAt", order: "desc"}]) {
        items {
          _contentTypeName
          _enabledAt
          title
          slug
          category { title }
          tout {
            image { path }
            dek
          }
        }
      }
    }
    social {
      title
      description
      image {
        path
      }
    }
  }

}
